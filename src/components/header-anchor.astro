---
interface Props {
  href: string;
  class?: string;
}

const { href, class: className } = Astro.props;
---

<a
  href={href}
  class=`block px-4 py-2 transition hover:shadow-inner hover:shadow-black/20 hover:scale-95 ${className}`
  ><slot /></a
>
